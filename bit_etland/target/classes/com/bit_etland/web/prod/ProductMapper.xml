<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
	"-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bit_etland.web.prod.ProductMapper">
<resultMap id="result" type="hashmap">
  <id property="productID" column="PRODUCT_ID" />
  <result property="productName" column="PRODUCT_NAME"/>
  <result property="unit" column="UNIT"/>
  <result property="photo" column="PHOTO"/>
  <result property="price" column="PRICE"/>
  <result property="rownum" column="ROWNUM"/>
</resultMap>
	<insert id="registProduct" parameterType="com.bit_etland.web.prod.Product"
		  flushCache="true" statementType="PREPARED" timeout="20">
	    INSERT INTO PRODUCTS (PRODUCT_NAME,SUPPLIER_ID,CATEGORY_ID,UNIT,PRICE) 
	    VALUES (#{productName},#{supplierID},#{categoryID},#{unit},#{price})
	</insert>
	<select id="bringProductList" resultMap="result"
	parameterType="com.bit_etland.web.cmm.Proxy">
	SELECT @ROWNUM:=@ROWNUM+1 ROWNUM, P.*
   	FROM PRODUCTS P, (SELECT @ROWNUM:=0)TMP
    ORDER BY ROWNUM DESC
    limit #{startRow}, #{pageSize}
	</select>
	<select id="retrieveProducts" resultType="com.bit_etland.web.prod.Product" 
	parameterType="com.bit_etland.web.cmm.Proxy">
	SELECT R.*
	FROM(SELECT @ROWNUM:=@ROWNUM+1 color,P.PRODUCT_NAME productName
			,S.SUPPLIER_NAME supplierID,P.UNIT,P.PRICE
		FROM PRODUCTS P JOIN SUPPLIERS S ON P.SUPPLIER_ID LIKE S.SUPPLIER_ID ,(SELECT @ROWNUM:=0)TMP
		WHERE P.PRODUCT_NAME LIKE #{search} 
			OR S.SUPPLIER_NAME LIKE #{search} 
			OR S.ADDRESS LIKE #{search}
			OR S.CITY LIKE #{search}) R
	LIMIT #{startRow}, #{pageSize}
	</select>
	<select id="searchcountProducts" resultType="int" parameterType="string">
	SELECT COUNT(*)
	FROM PRODUCTS P JOIN SUPPLIERS S ON P.SUPPLIER_ID LIKE S.SUPPLIER_ID
	WHERE P.PRODUCT_NAME LIKE #{SEARCH} 
		OR S.SUPPLIER_NAME LIKE #{SEARCH} 
		OR S.ADDRESS LIKE #{SEARCH}
		OR S.CITY LIKE #{SEARCH}
	</select>
	<select id="countProduct" resultType="Integer">
	SELECT COUNT(*)
	FROM PRODUCTS
	</select>
</mapper>